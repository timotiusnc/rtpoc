'use strict';

describe('Model: Project', function () {

  beforeEach(module('dsTmApp.models'));

  beforeEach(inject(function () { }));

  it('exists', function() { expect(true).toBe(true); });

  it('checking initial vars');

  it('add workspace will add a new workspace');

  it('add workspace when exceed MAX_WORKSPACE will return rejected promise')

  it('rem workspace should remove workspace');

  it('load workspace should load workspace');

  it('should return isLoadingWorkspace when loading workspace');
  
});
