'use strict';

/**
 * @ngdoc function
 * @name dsTmApp.config:translation en
 * @description
 * Default translation to be used (en).
 */

angular.module('dsTmApp.i18n')
  .config(function ($translateProvider, TM_lang) {
    $translateProvider.translations(TM_lang.en, {
      
    });
  });
